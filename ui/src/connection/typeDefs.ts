import contactsSchema from "../graphql/contacts/schema";
import profileSchema from "../graphql/profile/schema";
import errorSchema from "../graphql/error/schema";

export default [errorSchema, profileSchema, contactsSchema];
